# Пользовательские типы

## Часть 1: Пользовательские Типы (Инструменты для Структурирования Данных)

### `struct` (Структура) 🏗️
* **Пояснение:** Самый простой способ сгруппировать несколько переменных в одну логическую единицу. Все поля и методы по умолчанию `public`.
* **Код:**
```c++
#include <iostream>
#include <string>

struct Point {
    double x;
    double y;

    void print() {
        cout << "Point(" << x << ", " << y << ")" << endl;
    }
};

int main() {
    Point p1;
    p1.x = 10.5;
    p1.y = 20.0;
    p1.print();

    Point p2 = {5.0, -3.0};
    p2.print();

    return 0;
}
```
* **Разбор кода:** Создаем тип `Point`, который "упаковывает" `x` и `y`. `p1` - экземпляр этого типа. Доступ к полям прямой, так как они `public`.

---

### `class` (Класс) 🏛️
* **Пояснение:** Основной строительный блок ООП. Объединяет данные и поведение. Все поля и методы по умолчанию `private`. Реализует инкапсуляцию.
* **Код:**
```c++
#include <iostream>
#include <string>

class Person {
private:
    string name;
    int age;

public:
    Person(string n, int a) : name(n), age(a) {
        cout << "Объект " << name << " создан." << endl;
    }

    ~Person() {
        cout << "Объект " << name << " уничтожен." << endl;
    }

    void greet() {
        cout << "Привет, меня зовут " << name;
        cout << " и мне " << age << " лет." << endl;
    }

    int getAge() {
        return age;
    }

    void setAge(int newAge) {
        if (newAge > 0 && newAge < 120) {
            age = newAge;
        }
    }
};

int main() {
    Person alice("Алиса", 30);
    alice.greet();
    alice.setAge(31);
    alice.greet();
    alice.setAge(-5);
    alice.greet();

    return 0;
}
```
* **Разбор кода:** Поля `private` - защищены от прямого доступа. Конструктор инициализирует объект, деструктор очищает ресурсы. Методы `public` обеспечивают безопасный интерфейс.

---

### `enum class` (Перечисление класса) 🚦
* **Пояснение:** Тип с предопределенным списком именованных констант. Строго типизирован, обладает областью видимости.
* **Код:**
```c++
#include <iostream>

enum class Color {
    Red,
    Green,
    Blue
};

enum class TrafficLight {
    Red,
    Yellow,
    Green
};

void checkColor(Color c) {
    if (c == Color::Red) {
        std::cout << "Это красный цвет!" << std::endl;
    }
}

int main() {
    Color myColor = Color::Blue;
    checkColor(myColor);

    int colorValue = static_cast<int>(myColor);
    std::cout << "Значение Blue: " << colorValue << std::endl;

    return 0;
}
```
* **Разбор кода:** Обращение через `Color::Red`. Компилятор предотвращает смешивание разных типов перечислений.

---

### `union` (Объединение) 💾
* **Пояснение:** Особый `struct`, где все поля делят одну область памяти. Экономит память, но требует осторожности.
* **Код:**
```c++
#include <iostream>

union Value {
    int i;
    float f;
};

int main() {
    Value v;
    
    v.i = 10;
    std::cout << "v.i: " << v.i << std::endl;
    
    v.f = 3.14f;
    std::cout << "v.f: " << v.f << std::endl;
    std::cout << "v.i (мусор): " << v.i << std::endl;

    return 0;
}
```
* **Разбор кода:** При записи в одно поле другие становятся невалидными. Нужно отслеживать активный тип.

---

### `using` и `typedef` (Псевдонимы Типов) 🏷️
* **Пояснение:** Создание псевдонимов для существующих типов для улучшения читаемости.
* **Код:**
```c++
#include <vector>
#include <string>

typedef std::vector<int> IntVector;

using Name = std::string;
using Age = int;

void printName(Name n) { /* ... */ }
Age getAge() { return 30; }

int main() {
    IntVector vec = {1, 2, 3};
    Name myName = "Алекс";
    
    return 0;
}
```
* **Разбор кода:** `using` - современный способ создания псевдонимов. Упрощает сложные типы.

---

## Часть 2: Пользовательские Типы как Основа ООП

### Инкапсуляция (Encapsulation) 📦
* **Пояснение:** Сокрытие внутреннего состояния и предоставление доступа только через публичные методы.
* **Разбор кода:** В классе `Person` поля `private`, а методы `public`. `setAge` защищает данные от некорректных значений.

---

### Наследование (Inheritance) 🧬
* **Пояснение:** Создание нового класса на основе существующего. Отношение "является".
* **Код:**
```c++
#include <iostream>
#include <string>

class Person {
protected:
    std::string name;
    int age;
public:
    Person(std::string n, int a) : name(n), age(a) {}
    void greet() {
        std::cout << "Привет, я " << name << ", мне " << age << " лет." << std::endl;
    }
};

class Student : public Person {
private:
    std::string university;
public:
    Student(std::string n, int a, std::string u) 
        : Person(n, a), university(u) {}
    void study() {
        std::cout << name << " учится в " << university << "." << std::endl;
    }
};

int main() {
    Person bob("Боб", 40);
    bob.greet();

    Student carol("Кэрол", 20, "МГУ");
    carol.greet();
    carol.study();
    
    return 0;
}
```
* **Разбор кода:** `Student` наследует методы `Person` и добавляет свои. `protected` дает доступ наследникам.

---

### Полиморфизм (Polymorphism) 🎭
* **Пояснение:** Способность работать с объектами разных классов через единый интерфейс базового класса.
* **Код:**
```c++
#include <iostream>
#include <string>

class Person {
protected:
    std::string name;
public:
    Person(std::string n) : name(n) {}
    virtual void greet() {
        std::cout << "Привет, я " << name << "." << std::endl;
    }
    virtual ~Person() {} 
};

class Student : public Person {
private:
    std::string university;
public:
    Student(std::string n, std::string u) : Person(n), university(u) {}
    void greet() override {
        std::cout << "Привет, я студент " << name << " из " << university << "." << std::endl;
    }
};

void introduce(Person& p) {
    p.greet();
}

int main() {
    Person bob("Боб");
    Student carol("Кэрол", "МГУ");

    bob.greet();
    carol.greet();

    introduce(bob);
    introduce(carol);
    
    return 0;
}
```
* **Разбор кода:** Функция `introduce` принимает ссылку на базовый класс. Виртуальные методы обеспечивают вызов правильной реализации в зависимости от реального типа объекта.
