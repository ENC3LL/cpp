# ООП

## Основные принципы ООП

### 1. Инкапсуляция (Encapsulation) 📦
* **Пояснение:** Объединение данных и методов в одном объекте с сокрытием внутренней реализации. Доступ к данным контролируется через публичный интерфейс.
* **Код:**
```c++
#include <iostream>

class SmartThermos {
private:
    double currentTemperature;

public:
    explicit SmartThermos(double temp) : currentTemperature(temp) {}

    void heatUp(double amount) {
        if (amount > 0) {
            currentTemperature += amount;
        }
    }

    void coolDown(double amount) {
        if (amount > 0 && currentTemperature - amount > 0) {
            currentTemperature -= amount;
        }
    }

    double getTemperature() const {
        return currentTemperature;
    }
};

int main() {
    SmartThermos thermos(20.0);
    thermos.heatUp(15.5);
    thermos.coolDown(5.0);
    
    std::cout << "Current temperature: " << thermos.getTemperature() << " C" << std::endl;
    return 0;
}
```
* **Разбор кода:** Поле `currentTemperature` защищено от прямого доступа. Методы содержат логику проверки, предотвращая некорректные состояния.

---

### 2. Наследование (Inheritance) 🧬
* **Пояснение:** Создание нового класса на основе существующего. Дочерний класс перенимает члены родительского и может добавлять свои.
* **Код:**
```c++
#include <iostream>
#include <string>

class Vehicle {
public:
    std::string brand;
    explicit Vehicle(const std::string& b) : brand(b) {}
    void startEngine() const {
        std::cout << "Engine started for " << brand << std::endl;
    }
};

class Car : public Vehicle {
public:
    explicit Car(const std::string& b) : Vehicle(b) {}
    void drive() const {
        std::cout << brand << " is driving on the road." << std::endl;
    }
};

int main() {
    Car myCar("Tesla");
    myCar.startEngine(); // Унаследованный метод
    myCar.drive();       // Собственный метод
    return 0;
}
```
* **Разбор кода:** `Car` наследует `startEngine()` от `Vehicle` и добавляет собственный метод `drive()`.

---

### 3. Полиморфизм (Polymorphism) 🎭
* **Пояснение:** Способность объектов разных классов отвечать на один вызов метода по-разному через виртуальные функции.
* **Код:**
```c++
#include <iostream>
#include <memory>
#include <vector>

class Figure {
public:
    virtual void draw() const = 0;
    virtual ~Figure() = default;
};

class Circle : public Figure {
public:
    void draw() const override {
        std::cout << "Drawing a circle: O" << std::endl;
    }
};

class Rectangle : public Figure {
public:
    void draw() const override {
        std::cout << "Drawing a rectangle: [__]" << std::endl;
    }
};

void render(const Figure& figure) {
    figure.draw(); // Полиморфный вызов
}

int main() {
    Circle circle;
    Rectangle rect;
    render(circle); // Circle::draw()
    render(rect);   // Rectangle::draw()
    return 0;
}
```
* **Разбор кода:** Функция `render` работает с любым наследником `Figure`. Вызывается правильная версия `draw()` в зависимости от реального типа объекта.

---

### 4. Абстракция (Abstraction)
* **Пояснение:** Выделение значимых характеристик объекта через абстрактные классы, которые определяют интерфейс без полной реализации.
* **Разбор кода:** Класс `Figure` - абстрактный, требует от наследников реализации метода `draw()`.

---

## Дополнительные концепции ООП

### 5. Композиция и Агрегация
* **Пояснение:** Способы организации отношений между классами.
  - **Композиция:** отношение "часть-целое", где часть не может существовать без целого
  - **Агрегация:** более слабая связь, где часть может существовать независимо

* **Код (Композиция):**
```c++
class Engine {
public:
    void start() {
        std::cout << "Engine started" << std::endl;
    }
};

class Car {
private:
    Engine engine; // Композиция: двигатель - часть автомобиля
public:
    void startCar() {
        engine.start();
        std::cout << "Car is moving" << std::endl;
    }
};
```

* **Код (Агрегация):**
```c++
class Student {
public:
    std::string name;
    Student(const std::string& n) : name(n) {}
};

class University {
private:
    std::vector<Student*> students; // Агрегация: указатели на независимые объекты
public:
    void addStudent(Student* student) {
        students.push_back(student);
    }
};
```

---

### 6. Дружественные функции и классы
* **Пояснение:** Разрешение доступа к приватным членам для конкретных функций или классов.
* **Код:**
```c++
class Box {
private:
    double width;
public:
    Box(double w) : width(w) {}
    friend void printWidth(const Box& box); // Дружественная функция
    friend class BoxPrinter; // Дружественный класс
};

void printWidth(const Box& box) {
    std::cout << "Width: " << box.width << std::endl; // Доступ к private
}

class BoxPrinter {
public:
    void print(const Box& box) {
        std::cout << "Box width: " << box.width << std::endl;
    }
};
```

---

### 7. Статические члены класса
* **Пояснение:** Члены, принадлежащие самому классу, а не его экземплярам.
* **Код:**
```c++
class MyClass {
public:
    static int staticVar; // Статическая переменная
    static void staticFunction() { // Статическая функция
        std::cout << "StaticVar: " << staticVar << std::endl;
    }
};

int MyClass::staticVar = 0; // Определение статической переменной

int main() {
    MyClass::staticVar = 10; // Доступ через имя класса
    MyClass::staticFunction();
    return 0;
}
```

---

### 8. Внутренние классы (Nested Classes)
* **Пояснение:** Класс, объявленный внутри другого класса.
* **Код:**
```c++
class Outer {
private:
    int outerVar;
public:
    Outer(int val) : outerVar(val) {}
    class Inner { // Внутренний класс
    public:
        void showOuter(Outer& outer) {
            std::cout << "Outer variable: " << outer.outerVar << std::endl;
        }
    };
};
```

---

## Современные идиомы управления ресурсами

### 9. Правило Пяти (Rule of Five)
* **Пояснение:** Если определяете одну из пяти специальных функций, определяйте все:
  - Деструктор
  - Конструктор копирования
  - Копирующий оператор присваивания
  - Конструктор перемещения
  - Перемещающий оператор присваивания

* **Код:**
```c++
class CustomArray {
private:
    int* data_;
    size_t size_;
public:
    explicit CustomArray(size_t size) : size_(size), data_(new int[size]) {}
    ~CustomArray() { delete[] data_; } // 1. Деструктор
    CustomArray(const CustomArray& other) : size_(other.size_), data_(new int[other.size_]) { // 2. Копирование
        std::copy(other.data_, other.data_ + size_, data_);
    }
    CustomArray(CustomArray&& other) noexcept : data_(nullptr), size_(0) { // 3. Перемещение
        std::swap(data_, other.data_);
        std::swap(size_, other.size_);
    }
    CustomArray& operator=(const CustomArray& other) { // 4. Копирующее присваивание
        if (this != &other) {
            delete[] data_;
            size_ = other.size_;
            data_ = new int[size_];
            std::copy(other.data_, other.data_ + size_, data_);
        }
        return *this;
    }
    CustomArray& operator=(CustomArray&& other) noexcept { // 5. Перемещающее присваивание
        std::swap(data_, other.data_);
        std::swap(size_, other.size_);
        return *this;
    }
};
```

---

### 10. Правило Нуля (Rule of Zero) - Рекомендуемый подход
* **Пояснение:** Не управляйте ресурсами вручную. Используйте стандартные классы-обертки.
* **Код:**
```c++
#include <string>
#include <vector>
#include <memory>

class UserProfile {
private:
    std::string name_;
    std::vector<int> friends_ids_;
    std::unique_ptr<int> session_id_; // Умный указатель
public:
    UserProfile(std::string name, std::vector<int> ids, int session)
        : name_(std::move(name)), 
          friends_ids_(std::move(ids)), 
          session_id_(std::make_unique<int>(session)) {}
    // Никаких специальных функций - компилятор сделает всё правильно
};
```

---

### 11. Множественное наследование
* **Пояснение:** Наследование от нескольких базовых классов.
* **Код:**
```c++
class Animal {
public:
    void eat() { std::cout << "Eating..." << std::endl; }
};

class Mammal {
public:
    void breathe() { std::cout << "Breathing..." << std::endl; }
};

class Dog : public Animal, public Mammal { // Множественное наследование
public:
    void bark() { std::cout << "Barking..." << std::endl; }
};
```

---

### 12. Виртуальное наследование
* **Пояснение:** Решение проблемы ромбовидного наследования.
* **Код:**
```c++
class Animal {
public:
    void eat() { std::cout << "Eating..." << std::endl; }
};

class Mammal : virtual public Animal { // Виртуальное наследование
    // ...
};

class Bird : virtual public Animal { // Виртуальное наследование
    // ...
};

class Bat : public Mammal, public Bird {
    // Теперь только один экземпляр Animal
};
```

## Ключевые выводы

1. **Инкапсуляция** - защита данных через private/protected
2. **Наследование** - переиспользование кода через иерархии
3. **Полиморфизм** - единый интерфейс для разных типов
4. **Абстракция** - выделение существенных характеристик
5. **Правило Нуля** - предпочтительный современный подход
6. **Композиция предпочтительнее наследования** - более гибкая архитектура

Современный C++ поощряет использование умных указателей, стандартных контейнеров и следование Правилу Нуля для написания безопасного и поддерживаемого кода.