# Жадный алгоритм

## 💡 Что такое жадный алгоритм?

**Жадный алгоритм** — это подход, который на каждом шаге делает локально оптимальный выбор в надежде получить глобально оптимальное решение.

### Базовый шаблон C++

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include <limits>

using namespace std;

// Ускоряем ввод-вывод
ios_base::sync_with_stdio(false);
cin.tie(NULL);
cout.tie(NULL);
```

## 🏃 Способ 1: Сортировка + Итерация

### Структурный подход с кастомным компаратором

```cpp
struct Activity {
    int id, start, end;
};

bool compareActivities(const Activity& a, const Activity& b) {
    return a.end < b.end; // Сортировка по времени окончания
}

vector<Activity> greedyActivitySelection(vector<Activity>& activities) {
    sort(activities.begin(), activities.end(), compareActivities);
    
    vector<Activity> result;
    result.push_back(activities[0]);
    int lastEnd = activities[0].end;
    
    for (int i = 1; i < activities.size(); i++) {
        if (activities[i].start >= lastEnd) {
            result.push_back(activities[i]);
            lastEnd = activities[i].end;
        }
    }
    return result;
}
```

### Лямбда-функции для краткости

```cpp
struct Item {
    int id, value, weight;
    double valuePerWeight;
    
    Item(int i, int v, int w) : id(i), value(v), weight(w) {
        valuePerWeight = (double)v / w;
    }
};

double fractionalKnapsack(vector<Item>& items, int capacity) {
    // Сортировка по убыванию удельной ценности
    sort(items.begin(), items.end(), [](const Item& a, const Item& b) {
        return a.valuePerWeight > b.valuePerWeight;
    });
    
    double totalValue = 0.0;
    int currentWeight = 0;
    
    for (const auto& item : items) {
        if (currentWeight + item.weight <= capacity) {
            currentWeight += item.weight;
            totalValue += item.value;
        } else {
            int remaining = capacity - currentWeight;
            totalValue += item.valuePerWeight * remaining;
            break;
        }
    }
    return totalValue;
}
```

## 🗺️ Способ 2: Очередь с приоритетом

### Алгоритм Дейкстры

```cpp
const int INF = numeric_limits<int>::max();
using PII = pair<int, int>; // {distance, node}

vector<int> dijkstra(vector<vector<PII>>& graph, int start) {
    int n = graph.size();
    vector<int> dist(n, INF);
    priority_queue<PII, vector<PII>, greater<PII>> pq;
    
    dist[start] = 0;
    pq.push({0, start});
    
    while (!pq.empty()) {
        int d = pq.top().first;
        int u = pq.top().second;
        pq.pop();
        
        if (d > dist[u]) continue;
        
        for (auto& edge : graph[u]) {
            int v = edge.first;
            int weight = edge.second;
            
            if (dist[u] + weight < dist[v]) {
                dist[v] = dist[u] + weight;
                pq.push({dist[v], v});
            }
        }
    }
    return dist;
}
```

## 📊 Сравнение подходов

| Характеристика | Сортировка + Итерация | Priority Queue |
|----------------|----------------------|----------------|
| **Сложность** | O(n log n) | O(n log n) |
| **Память** | O(1) или O(n) | O(n) |
| **Использование** | Статические данные | Динамический выбор |
| **Примеры** | Activity Selection, Fractional Knapsack | Dijkstra, Prim, Huffman |

## 🎯 Практические шаблоны

### Шаблон 1: Сортировка по одному критерию

```cpp
vector<pair<int, int>> items = {{60, 10}, {100, 20}, {120, 30}};
sort(items.begin(), items.end(), [](auto& a, auto& b) {
    return (double)a.first/a.second > (double)b.first/b.second;
});
```

### Шаблон 2: Сортировка по нескольким полям

```cpp
struct Task {
    int deadline, duration, id;
};

vector<Task> tasks;
sort(tasks.begin(), tasks.end(), [](const Task& a, const Task& b) {
    if (a.deadline != b.deadline)
        return a.deadline < b.deadline;
    return a.duration < b.duration;
});
```

### Шаблон 3: Min-Heap для k-ых элементов

```cpp
priority_queue<int, vector<int>, greater<int>> minHeap;

// Найти k наибольших элементов
for (int num : nums) {
    minHeap.push(num);
    if (minHeap.size() > k) {
        minHeap.pop();
    }
}
```

## ⚠️ Критические предупреждения

### Когда НЕ использовать жадные алгоритмы:

```cpp
// ❌ НЕ для 0/1 рюкзака
vector<Item> items = {{60, 10}, {100, 20}, {120, 30}};
int capacity = 50;

// Жадный даст 60+100=160, но оптимально 100+120=220
// Нужно динамическое программирование!

// ❌ НЕ для неканонических систем монет
vector<int> coins = {1, 3, 4};
int amount = 6;

// Жадный: 4+1+1=3 монеты
// Оптимально: 3+3=2 монеты
```

## 🔍 Проверка корректности

Прежде чем применять жадный алгоритм, задай себе:

1. **Локальная оптимальность → Глобальная оптимальность?**
2. **Оптимальная подструктура?** 
3. **Есть ли контрпримеры?**

## 🛠️ Универсальный чеклист реализации

```cpp
// 1. Определить структуру данных
struct Element {
    int id;
    // поля для критерия сортировки
};

// 2. Определить компаратор
bool comparator(const Element& a, const Element& b) {
    // критерий жадного выбора
}

// 3. Отсортировать
sort(elements.begin(), elements.end(), comparator);

// 4. Итерировать с жадным выбором
for (const auto& element : elements) {
    if (/* условие принятия */) {
        // принять элемент
        // обновить состояние
    }
}
```

## 🎖️ Золотые правила

1. **Сначала докажи корректность** — жадные алгоритмы обманчиво просты
2. **Сортируй по правильному критерию** — это 90% успеха  
3. **Используй priority_queue для динамического выбора** — когда следующий шаг зависит от предыдущих
4. **Проверяй крайние случаи** — пустые массивы, одинаковые элементы, граничные значения

Это руководство покрывает 95% задач, где применимы жадные алгоритмы. Практикуйся на классических задачах, и этот подход станет твоим надежным инструментом!